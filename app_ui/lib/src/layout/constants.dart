import 'package:flutter/material.dart';

/// Default constants for most of the layout
abstract class ZeeveConstants {
  /// radius for cards and tiles
  static const BorderRadius radius = BorderRadius.all(Radius.circular(25));
}
